version: '3.8'

services:
  hardtruth-daemon:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: hardtruth-daemon
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
